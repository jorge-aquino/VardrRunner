"""VardrRunner — local automation runner for the VardrSec product family.

The version literal below is the single source of truth: `pyproject.toml` reads
it via setuptools' dynamic-version mechanism, and the heartbeat reports it to the
backend. Bump it here (and add a CHANGELOG entry) on every release.
"""

__version__ = "0.24.0"
